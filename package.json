{
  "name": "neon-editor",
  "private": true,
  "version": "0.1.0",
  "description": "Animated flowing neon border and keyword glow for the pi input editor.",
  "type": "module",
  "keywords": [
    "pi-package",
    "pi-extension",
    "editor",
    "neon",
    "tui"
  ],
  "scripts": {
    "check": "pi -e . --version"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*",
    "@earendil-works/pi-tui": "*"
  }
}
